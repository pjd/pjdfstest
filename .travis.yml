language: c
sudo: required

matrix:
    include:
        - os: linux
          compiler: clang
          dist: xenial
        - os: linux
          compiler: gcc
          dist: xenial

script:
    - ./travis/build.sh
    - ./travis/test.sh
